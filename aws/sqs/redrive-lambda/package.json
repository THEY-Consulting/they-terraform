{
  "name": "redrive-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --sourcemap --target=node20 --outdir=dist --keep-names"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.840.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "esbuild": "^0.25.4",
    "typescript": "^5.8"
  }
}
