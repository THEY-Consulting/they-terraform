{
  "name": "@they/example-bundle-lambda",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --noemit && esbuild index.ts --bundle --platform=node --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "5.2.2"
  }
}
